#include<bits/stdc++.h>
using namespace std;

int main(){

    int arr[] = {7,3,1,5,64,21};
    // auto a = arr;
    // list<int>a = {7,3,2,4,5,2};

    // bool res = binary_search(a, a+n, 3);
    // bool r = binary_search(arr, arr+n, 34);
    // int lb  = lower_bound(arr, arr+n, 21) - arr;
    // int ub = upper_bound(arr, arr+n, 21) - arr;
    // return 0;
}